# AGENTS.md

## Cursor Cloud specific instructions

This is a greenfield diploma project ("Diplom") intended to be a website with 3D model visualization. As of this writing the repository contains only a `README.md`; no application code, dependencies, or infrastructure exist yet.

- **No services to start.** Once code is added, update this section with startup/run commands.
- **No dependencies to install.** Once a `package.json`, `requirements.txt`, or equivalent is added, update the VM update script accordingly.
- **No lint/test/build commands.** These should be documented here once the tech stack is chosen and code is committed.
