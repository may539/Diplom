#!/usr/bin/env python3
"""Generate РАЗДЕЛ 5 — Контрольный пример (Word) for the Diplom web app."""

from pathlib import Path

from docx import Document
from docx.enum.text import WD_ALIGN_PARAGRAPH
from docx.shared import Cm, Pt

OUTPUT = Path(__file__).resolve().parent.parent / "docs" / "Раздел_5_Контрольный_пример.docx"


def add_figure_placeholder(doc, number, caption):
    p = doc.add_paragraph()
    p.alignment = WD_ALIGN_PARAGRAPH.CENTER
    run = p.add_run(f"[ Место для рисунка {number} ]")
    run.italic = True
    run.font.size = Pt(11)
    cap = doc.add_paragraph(f"Рисунок {number} – {caption}")
    cap.alignment = WD_ALIGN_PARAGRAPH.CENTER
    cap.runs[0].italic = True
    doc.add_paragraph()


def add_heading(doc, text, level=1):
    doc.add_heading(text, level=level)


def add_body(doc, text):
    doc.add_paragraph(text)


def add_numbered_list(doc, items):
    for item in items:
        doc.add_paragraph(item, style="List Number")


def build_document():
    doc = Document()

    section = doc.sections[0]
    section.top_margin = Cm(2)
    section.bottom_margin = Cm(2)
    section.left_margin = Cm(3)
    section.right_margin = Cm(1.5)

    title = doc.add_heading("РАЗДЕЛ 5 КОНТРОЛЬНЫЙ ПРИМЕР", level=0)
    title.alignment = WD_ALIGN_PARAGRAPH.CENTER

    add_body(
        doc,
        "После запуска веб-приложения (команда npm start или docker compose up -d) "
        "в браузере открывается главная страница по адресу http://<адрес-сервера>:8080/. "
        "Ниже приведён пошаговый контрольный пример работы с системой «3D-визуализация "
        "учебного оборудования».",
    )

    add_body(
        doc,
        "После старта программы можно видеть главную страницу, изображённую на рисунке 16.",
    )
    add_figure_placeholder(doc, 16, "Главная страница веб-приложения")

    add_body(doc, "На главной странице можно увидеть:")
    add_numbered_list(
        doc,
        [
            "Шапку сайта. Содержит логотип «3D Учебное оборудование» и кнопку меню "
            "(три горизонтальные полоски — «бургер») в правом верхнем углу.",
            "Блок приветствия (hero). Краткое описание назначения сайта и кнопки "
            "«Выбрать специальность» и «Открыть 3D просмотр» для быстрого перехода к разделам.",
            "Панель возможностей. Три карточки: «Каталог», «3D просмотр», «QR-доступ».",
            "Раздел «Выберите специальность». Карточки специальностей ОИБ, ПД и ЗЕМ. "
            "При выборе специальности обновляется список оборудования и загружается первая 3D-модель.",
            "Раздел «Оборудование и 3D модель». Слева — список объектов выбранной "
            "специальности; справа — интерактивный просмотрщик (компонент model-viewer), "
            "название, описание, список особенностей и кнопки управления (сетка, QR, ссылка на GLB).",
            "Раздел «QR-код для реального оборудования». Кнопка генерации QR для текущей модели.",
            "Подвал страницы. Ссылка на репозиторий GitHub и кнопка «Политика конфиденциальности».",
        ],
    )

    # --- Menu ---
    add_heading(doc, "Работа с меню «бургер»", level=2)
    add_body(
        doc,
        "Для открытия служебного меню нажмите левой кнопкой мыши на кнопку с тремя "
        "полосками в правом верхнем углу, как на рисунке 17. Список пунктов появляется "
        "только после нажатия; повторное нажатие или клик вне меню закрывает его.",
    )
    add_figure_placeholder(doc, 17, "Кнопка меню (бургер) в шапке сайта")

    add_body(doc, "В выпадающем списке доступны три пункта (рисунок 18):")
    add_numbered_list(
        doc,
        [
            "«Загрузить модель» — открывает модальное окно админ-панели;",
            "«База знаний (FAQ)» — открывает справочные статьи из базы SQLite;",
            "«О нас» — открывает информацию о проекте.",
        ],
    )
    add_figure_placeholder(doc, 18, "Выпадающее меню с тремя пунктами")

    # --- Specialty ---
    add_heading(doc, "Выбор специальности и просмотр оборудования", level=2)
    add_body(
        doc,
        "Прокрутите страницу до раздела «Выберите специальность» (рисунок 19) "
        "и нажмите левой кнопкой мыши на карточку нужной специальности, например «ЗЕМ».",
    )
    add_figure_placeholder(doc, 19, "Раздел выбора специальности")

    add_body(
        doc,
        "Страница автоматически прокрутится к блоку 3D-просмотра. В левой колонке "
        "отобразится список оборудования выбранной специальности (рисунок 20). "
        "Нажмите на любой объект в списке — справа обновятся название, описание, "
        "особенности и загрузится соответствующая GLB-модель в просмотрщик.",
    )
    add_figure_placeholder(doc, 20, "Список оборудования и область 3D-просмотра")

    add_body(
        doc,
        "Управление 3D-моделью (рисунок 21): перетаскивание мышью — вращение; "
        "колесо мыши или кнопки «+» / «−» / «100%» — масштаб; кнопка «Сетка» — "
        "режим каркасного отображения (если активна резервная CSS-сцена). "
        "На сенсорном экране используются жесты компонента model-viewer (вращение, масштаб).",
    )
    add_figure_placeholder(doc, 21, "Интерактивный 3D-просмотрщик с элементами управления")

    # --- QR ---
    add_heading(doc, "Генерация QR-кода", level=2)
    add_body(
        doc,
        "Выберите оборудование в списке. Нажмите кнопку «Показать QR-код» в карточке "
        "модели или кнопку «Сгенерировать QR для текущей модели» в нижнем разделе страницы, "
        "как на рисунке 22.",
    )
    add_figure_placeholder(doc, 22, "Кнопки вызова QR-кода")

    add_body(
        doc,
        "Откроется модальное окно (рисунок 23) с изображением QR-кода и текстовой ссылкой "
        "вида http://<адрес-сервера>/?equipment=<идентификатор>. Ссылка формируется "
        "автоматически из window.location.origin, поэтому QR работает на localhost, "
        "IP виртуальной машины и домене колледжа. QR-код можно распечатать и разместить "
        "на учебном стенде.",
    )
    add_figure_placeholder(doc, 23, "Модальное окно с QR-кодом и ссылкой")

    # --- Kiosk ---
    add_heading(doc, "Режим просмотра по QR (киоск)", level=2)
    add_body(
        doc,
        "Отсканируйте QR-код смартфоном или откройте в браузере ссылку с параметром "
        "?equipment=<id>, например: http://192.168.1.10:8080/?equipment=terrain-relief. "
        "Лендинг скрывается, на весь экран отображается только 3D-модель выбранного "
        "оборудования (рисунок 24). Внизу — компактная панель с названием и кнопками "
        "«К каталогу» и «Описание».",
    )
    add_figure_placeholder(doc, 24, "Полноэкранный режим просмотра после сканирования QR")

    add_body(
        doc,
        "Кнопка «К каталогу» возвращает на главную страницу с полным каталогом. "
        "Кнопка «Описание» показывает или скрывает текстовое описание объекта поверх 3D-сцены.",
    )

    # --- Admin ---
    add_heading(doc, "Добавление 3D-модели через админ-панель", level=2)
    add_body(
        doc,
        "Нажмите кнопку меню (бургер) и выберите пункт «Загрузить модель», как на рисунке 25.",
    )
    add_figure_placeholder(doc, 25, "Вызов админ-панели из меню")

    add_body(
        doc,
        "Откроется модальное окно «Загрузить модель». На первом шаге (рисунок 26) "
        "введите мастер-пароль администратора (по умолчанию admin123, на сервере задаётся "
        "переменной ADMIN_PASSWORD) и нажмите «Открыть форму». При неверном пароле "
        "появится сообщение об ошибке.",
    )
    add_figure_placeholder(doc, 26, "Шаг ввода пароля администратора")

    add_body(
        doc,
        "На втором шаге заполните форму (рисунок 27): специальность, название, тип, "
        "краткое и полное описание, особенности (каждая с новой строки), ссылку на GLB "
        "или загрузите файл .glb / .gltf с диска (до 50 МБ). При необходимости укажите "
        "вариант 3D-формы и окружение. Нажмите «Сохранить и получить QR».",
    )
    add_figure_placeholder(doc, 27, "Форма добавления новой 3D-модели")

    add_body(
        doc,
        "При успешном сохранении откроется третий шаг (рисунок 28): QR-код для новой "
        "модели, ссылка для проверки и кнопки «Печать QR» и «Добавить ещё». "
        "Данные сохраняются в базу SQLite (data/equipment.sqlite); загруженный файл — "
        "в каталог models/uploads/. Новая модель сразу появляется в каталоге на главной странице.",
    )
    add_figure_placeholder(doc, 28, "Успешное добавление модели и QR для печати")

    add_body(
        doc,
        "Внимание: пароль администратора не следует оставлять по умолчанию на "
        "рабочем сервере учебного заведения. Рекомендуется задать ADMIN_PASSWORD "
        "в файле .env перед запуском Docker.",
    )

    # --- FAQ ---
    add_heading(doc, "База знаний (FAQ)", level=2)
    add_body(
        doc,
        "В меню «бургер» выберите «База знаний (FAQ)». Откроется модальное окно "
        "(рисунок 29) со списком статей, загруженных с сервера (GET /api/help-articles). "
        "Нажмите на заголовок статьи — раскроется текст с инструкциями (QR-режим, "
        "добавление моделей, форматы GLB, доступ администратора, развёртывание на Linux-VM).",
    )
    add_figure_placeholder(doc, 29, "Модальное окно «База знаний (FAQ)»")

    # --- About & Privacy ---
    add_heading(doc, "Разделы «О нас» и политика конфиденциальности", level=2)
    add_body(
        doc,
        "Пункт меню «О нас» открывает модальное окно с описанием дипломного проекта, "
        "используемого стека (Node.js, Express, SQLite, model-viewer) и принципов "
        "хранения данных (рисунок 30).",
    )
    add_figure_placeholder(doc, 30, "Модальное окно «О нас»")

    add_body(
        doc,
        "В подвале сайта нажмите «Политика конфиденциальности» (рисунок 31). "
        "Откроется модальное окно с текстом о сборе технических журналов (scan.log), "
        "данных администратора и рекомендациях по защите сервера.",
    )
    add_figure_placeholder(doc, 31, "Модальное окно «Политика конфиденциальности»")

    add_body(
        doc,
        "Кнопка «GitHub» в подвале открывает в новой вкладке репозиторий проекта "
        "https://github.com/may539/Diplom (рисунок 32).",
    )
    add_figure_placeholder(doc, 32, "Подвал сайта со ссылкой на GitHub")

    # --- Deploy note ---
    add_heading(doc, "Запуск на сервере (контрольная проверка)", level=2)
    add_body(
        doc,
        "Для проверки работоспособности на виртуальной машине выполните: "
        "git clone <репозиторий>; cd <каталог>; cp .env.example .env; "
        "отредактируйте ADMIN_PASSWORD; docker compose up -d --build. "
        "Убедитесь, что в браузере открывается главная страница, API отвечает "
        "(GET /api/specialties — код 200), а после добавления модели через админку "
        "она отображается в каталоге и открывается по QR-ссылке.",
    )

    add_body(
        doc,
        "Контрольный пример завершён. Все перечисленные операции подтверждают "
        "соответствие веб-приложения функциональным требованиям: каталог специальностей, "
        "динамическая загрузка 3D-моделей из SQLite, QR-доступ, админ-панель с загрузкой "
        "файлов, справочная система и размещение на Linux-сервере в Docker.",
    )

    OUTPUT.parent.mkdir(parents=True, exist_ok=True)
    doc.save(OUTPUT)
    return OUTPUT


if __name__ == "__main__":
    path = build_document()
    print(f"Saved: {path}")
